import styles from './config.css';
import { Config, ConfigPanel, ConfigInput, ConfigFooter } from './config';
export {
  Config as Root,
  ConfigPanel as Panel,
  ConfigInput as Input,
  ConfigFooter as Footer,
  styles,
};
